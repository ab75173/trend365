
<h2>Search Results</h2>
  <ul>
    <% if @results %>
      <% @results.each do |article| %>
        <li>
          <%= link_to article[:headline], article_path(article[:id]) %>
        </li>
      <% end %>
    <% else %>
      <p>No results found.</p>
    <% end %>
  </ul>
